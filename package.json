{
  "name": "meyson-boilerplate",
  "description": "A comprehensive, opinionated company monorepo boilerplate",
  "license": "MIT",
  "author": "Meyson",
  "version": "0.0.1",
  "packageManager": "bun@1.2.22",
  "workspaces": [
    "apps/*",
    "packages/*",
    "tooling/*"
  ],
  "scripts": {
    "build": "turbo build",
    "build:backend": "turbo build --filter=@meyson/backend",
    "build:web": "turbo build --filter=@meyson/web",
    "build:marketing": "turbo build --filter=@meyson/marketing",
    "dev": "turbo dev",
    "dev:backend": "turbo dev --filter=@meyson/backend",
    "dev:web": "turbo dev --filter=@meyson/web",
    "dev:marketing": "turbo dev --filter=@meyson/marketing",
    "format-and-lint": "biome check .",
    "format-and-lint:fix": "biome check . --fix",
    "typecheck": "turbo typecheck",
    "update-deps": "bunx npm-check-updates --workspaces",
    "update-deps:check": "bunx npm-check-updates --workspaces",
    "update-deps:upgrade": "bunx npm-check-updates -u --workspaces"
  },
  "dependencies": {},
  "devDependencies": {
    "@biomejs/biome": "^2.2.4",
    "turbo": "^2.5.6"
  }
}
